{
  "name": "qa-automation-audit-toolkit",
  "version": "1.0.0",
  "description": "Evidence-based static audit toolkit for Playwright and Cypress test suites.",
  "type": "module",
  "bin": {
    "qa-audit": "dist/index.js"
  },
  "scripts": {
    "audit": "node --import tsx src/index.ts",
    "demo": "node --import tsx src/index.ts examples/sample-suite --format both --output reports",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run typecheck && npm run format:check && npm test"
  },
  "keywords": [
    "qa-audit",
    "playwright",
    "cypress",
    "test-automation",
    "quality-engineering"
  ],
  "author": "Gonzalo Alberto Diamend",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prettier": "^3.6.0",
    "tsx": "^4.20.0",
    "typescript": "7.0.2",
    "vitest": "^3.2.0"
  }
}
